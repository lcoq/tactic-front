<header class="header it-header">
  <div class="header-brand-container">
    <h1 class="header-brand">Tactic</h1>
    {{#if isAuthenticated}}
      {{#link-to "login" class="header-brand-user it-current-user no-print"}}@ {{currentUserName}}{{/link-to}}
    {{/if}}
  </div>

  {{#if isAuthenticated}}
    <nav class="header-nav no-print">
      {{current-week-summary}}
      <section class="header-nav-section">
        {{#link-to "index" class="header-nav-section-title touch"}}
          Timer
        {{/link-to}}
      </section>
      <section class="header-nav-section">
        {{#link-to "reviews" class="header-nav-section-title touch"}}
          Reviews
        {{/link-to}}
      </section>
      <section class="header-nav-section right">
        {{#link-to "projects" class="header-nav-section-title touch"}}
          Projects
        {{/link-to}}
      </section>
    </nav>
  {{/if}}

</header>

<div class="app-content">
  {{outlet}}
</div>
