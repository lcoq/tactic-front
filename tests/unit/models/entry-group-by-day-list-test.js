import { moduleFor, test } from 'ember-qunit';

moduleFor('model:entry-group-by-day-list', 'Unit | Model | entry group by day list', {
  unit: true,
  needs: []
});

test('it exists', function(assert) {
  let model = this.subject();
  assert.ok(!!model);
});
