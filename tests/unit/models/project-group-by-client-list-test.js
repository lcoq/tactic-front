import { moduleFor, test } from 'ember-qunit';

moduleFor('model:project-group-by-client-list', 'Unit | Model | project group by client list', {
  needs: []
});

test('it exists', function(assert) {
  let model = this.subject();
  assert.ok(!!model);
});
