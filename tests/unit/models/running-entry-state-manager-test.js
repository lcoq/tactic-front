import { moduleFor, test } from 'ember-qunit';

moduleFor('model:running-entry-state-manager', 'Unit | Model | running entry state manager', {
  unit: true,
  needs: []
});

test('it exists', function(assert) {
  let model = this.subject();
  assert.ok(!!model);
});
