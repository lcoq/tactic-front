//import scheduleOnce from 'tactic-front/utils/schedule-once';
import { module, test } from 'qunit';

module('Unit | Utility | schedule once');

// Replace this with your real tests.
test('it works', function(assert) {
  assert.expect(0);
});
