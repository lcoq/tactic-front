import { moduleForComponent, test } from 'ember-qunit';
//import hbs from 'htmlbars-inline-precompile';

moduleForComponent('current-week-summary', 'Integration | Component | current week summary', {
  integration: true
});

test('it renders', function(assert) {
  assert.expect(0);
  //this.render(hbs`{{current-week-summary}}`);
});
