import { moduleForComponent, test } from 'ember-qunit';
//import hbs from 'htmlbars-inline-precompile';

moduleForComponent('user-summary-summary', 'Integration | Component | user summary', {
  integration: true
});

test('it renders', function(assert) {
  assert.expect(0);
  //this.render(hbs`{{user-summary-summary}}`);
});
